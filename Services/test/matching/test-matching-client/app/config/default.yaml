# test client
#testClient:

# mindlink client
mindlinkClient:
  url: "ws://mindlink:7766"

# matching client
matchingClient:
  url: "ws://matching:7755"

# log4js
log4js:
  appenders:
    console:
      type: console
  categories:
    default:
      appenders: [console]
      level: [info, "${LOG_LEVEL}"]
    testClient:
      appenders: [console]
      level: [info, "${LOG_LEVEL}"]
    mindlinkClient:
      appenders: [console]
      level: [info, "${LOG_LEVEL}"]
    matchingClient:
      appenders: [console]
      level: [info, "${LOG_LEVEL}"]
