# test client
#testClient:

# mindlink client
#mindlinkClient:
#  url: "ws://mindlink:7766"

# log4js
log4js:
  appenders:
    console:
      type: console
  categories:
    default:
      appenders: [console]
      level: [info, "${LOG_LEVEL}"]
    testClient:
      appenders: [console]
      level: [info, "${LOG_LEVEL}"]
