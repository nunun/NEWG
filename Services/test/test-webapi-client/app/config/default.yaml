# test client
#testClient:

# webapi client
webapiClient:
  url: "http://api:7780"

# log4js
log4js:
  appenders:
    console:
      type: console
  categories:
    default:
      appenders: [console]
      level: [info, "${LOG_LEVEL}"]
    testClient:
      appenders: [console]
      level: [info, "${LOG_LEVEL}"]
    webapiClient:
      appenders: [console]
      level: [info, "${LOG_LEVEL}"]
